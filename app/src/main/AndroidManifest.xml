<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      INTERNET is the ONLY permission this app requests.
      It is used solely by the secondary Match Schedule screen to load football
      match data from football-data.org. No location, camera, microphone,
      contacts, storage, notifications, sensors, Google Fit, Health Connect,
      wearable, calendar, alarms, or background-work permissions are declared.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".TeamCaptainApp"
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TeamCaptainNotes.Splash">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:theme="@style/Theme.TeamCaptainNotes.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
